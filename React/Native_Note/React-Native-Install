<Native에서 쓰여지는 install 모듈 종류>



● react-native-gesture-handler

- 제스처 기반의 사용자 인터페이스를 만들 수 있도록 해주는 라이브러리
  기본적인 터치, 스와이프, 드래그와 같은 제스처를 인식하고 처리할 수 있다.

● react-native-reanimated

- 애니메이션을 효율적으로 규현할 수 있도록 돕는 라이브러리.

● react-native-screens

- 앱에서 화면 이동과 관련된 자원을 더 효율적으로 관리해서 화면 전환을
  더욱 매끄럽게 해주는 라이브러리.

● react-native-safe-area-context

- 아이폰X 이후 기기의 상단에 노치나 인디케이터 등을 고려해서 레이아웃을
  안전하게 조정하는 라이브러리.

● react-native-community/masked-view
- 뷰에 마스크(mask)를 씌울 수 있도록 해주는 라이브러리
  네비게이션과 함께 사용해서 전환 효과를 더 자연스럽게 만들 때 유용하다.

● npm install @react-navigation/bottom-tabs@6.5.20
- 탭을 누르면 버튼과 연결된 화면으로 이동하는 방식으로 동작

-buildLink is not function
